# Infisical machine identity credentials.
# Copy to .env.bootstrap, fill in values from the Infisical UI.
# All app secrets are fetched from Infisical at runtime — nothing else needed here.
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENV=prod
